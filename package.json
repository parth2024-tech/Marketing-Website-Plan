{
  "name": "@senitel/marketing-website-plan",
  "version": "1.0.0",
  "description": "Senitel Marketing Website and API Workspace",
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  },
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "if [ \"$CI\" = \"true\" ] || [ -n \"$RENDER\" ]; then pnpm run build:api; else pnpm run typecheck && pnpm -r --if-present run build; fi",
    "build:api": "pnpm --filter @workspace/api-server build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "node -e \"const cp = require('child_process'); cp.spawnSync('pnpm', ['vitest', 'run'], { stdio: 'inherit' })\" --",
    "format": "prettier --write .",
    "lint": "eslint .",
    "audit": "pnpm audit"
  },
  "private": true,
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.6.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.59.4",
    "vitest": "^4.1.6"
  }
}
