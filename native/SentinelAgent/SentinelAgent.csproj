<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <SupportedOSPlatformVersion>10.0.17763.0</SupportedOSPlatformVersion>
  </PropertyGroup>
  <!-- Non-production default base URL (still overridable with SENTINEL_API_BASE_URL at runtime): -->
  <PropertyGroup Condition="'$(SentinelStaging)' == 'true'">
    <DefineConstants>$(DefineConstants);SENTINEL_STAGING</DefineConstants>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Shared\Collector\Collector.csproj" />
    <ProjectReference Include="..\Shared\Uploader\Uploader.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Win32.Registry" Version="5.0.0" />
  </ItemGroup>
</Project>
